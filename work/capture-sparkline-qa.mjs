import fs from 'node:fs';
const endpoint=process.argv[2],width=Number(process.argv[3]),height=Number(process.argv[4]),output=process.argv[5];
const delay=ms=>new Promise(resolve=>setTimeout(resolve,ms));let page;
for(let i=0;i<30;i+=1){try{page=(await fetch(endpoint+'/json').then(r=>r.json())).find(item=>item.type==='page');if(page)break}catch{}await delay(200)}
if(!page)throw new Error('No debugging page');const socket=new WebSocket(page.webSocketDebuggerUrl);await new Promise((resolve,reject)=>{socket.addEventListener('open',resolve,{once:true});socket.addEventListener('error',reject,{once:true})});
let id=0;const pending=new Map();socket.addEventListener('message',event=>{const message=JSON.parse(event.data);if(!pending.has(message.id))return;const task=pending.get(message.id);pending.delete(message.id);message.error?task.reject(new Error(message.error.message)):task.resolve(message.result)});const send=(method,params={})=>new Promise((resolve,reject)=>{const call=++id;pending.set(call,{resolve,reject});socket.send(JSON.stringify({id:call,method,params}))});
await send('Page.enable');await send('Runtime.enable');await send('Emulation.setDeviceMetricsOverride',{width,height,deviceScaleFactor:1,mobile:false});await send('Page.navigate',{url:'http://localhost:4173/'});await delay(700);await send('Runtime.evaluate',{expression:"localStorage.setItem('demo-authenticated','true');localStorage.setItem('demo-role','集团');"});await send('Page.navigate',{url:'http://localhost:4173/cockpit'});await delay(1400);const shot=await send('Page.captureScreenshot',{format:'png',captureBeyondViewport:false,fromSurface:true});fs.writeFileSync(output,Buffer.from(shot.data,'base64'));socket.close();console.log(output);
