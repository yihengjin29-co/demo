# 金控并表管理 DEMO（核心代码版）

## 环境要求

- Node.js `20.19+` 或 `22.12+`
- npm

## 本地运行

```bash
npm install
npm run dev
```

启动后访问终端中显示的地址，默认通常为：

```text
http://localhost:5173/
```

## 构建检查

```bash
npm run build
```

## 精简说明

压缩包保留了可运行所需的 `src`、`public`、HTML 入口、依赖清单及 TypeScript/Vite 配置。
未包含可重新生成或与运行无关的 `.git`、`node_modules`、`dist`、`work`、`docs`、`isolated-demos`、开发提示词和日志文件。
