import type { DemoState, PeriodicReport } from '../types';
import { createLog, uid } from './storage';
export const periodicReportService = { create(state: DemoState, value: Partial<PeriodicReport>, operator: string) { const item: PeriodicReport = { id: uid('pr'), name: value.name || '未命名定期风险报告', institution: value.institution || '上海农商银行', type: value.type || '全面风险管理报告', reportDate: value.reportDate || '', submitDate: new Date().toISOString().slice(0, 10), cycle: value.cycle || '月度', overview: value.overview || '', attachments: value.attachments || [], visibleInstitutions: value.visibleInstitutions || ['上海农商银行'], visibleDepartments: value.visibleDepartments || [], viewers: value.viewers || [], pushMethod: value.pushMethod || '平台通知', status: '草稿', logs: [createLog('保存草稿', '创建定期风险报告', operator)] }; state.periodicReports.unshift(item); return item; } };
